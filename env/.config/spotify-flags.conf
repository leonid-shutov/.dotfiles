# Run Spotify as a native Wayland client so Hyprland handles scaling via
# wp_fractional_scale_v1. Without this, Spotify is the lone XWayland client and
# renders at 1x under the 2.5 display scale (see ~/.config/hypr/monitors.lua).
--ozone-platform=wayland
